[phases.setup]
nixPkgs = ["nodejs_20", "python312"]

[phases.build]
cmds = [
  "pip install uv",
  "uv sync --frozen",
  "cd web && npm install && npm run build",
]

[phases.start]
cmd = "uv run cortex serve --host 0.0.0.0 --port $PORT"
