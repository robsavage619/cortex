# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
# NOTE: web/dist IS intentionally committed — the Railway/NIXPACKS build only
# builds Python and serves this prebuilt SPA bundle. Do not ignore it here, or
# rebuilt assets get silently dropped from commits (root .gitignore re-includes it).
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
