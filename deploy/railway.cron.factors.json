{
  "$schema": "https://schema.railway.app/railway-schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "uv run cortex trigger-snapshot",
    "cronSchedule": "0 8 * * *",
    "restartPolicyType": "NEVER"
  }
}
