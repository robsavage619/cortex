{
  "$schema": "https://schema.railway.app/railway-schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "uv run cortex trigger-refresh --only funds",
    "cronSchedule": "0 7 * * 1",
    "restartPolicyType": "NEVER"
  }
}
