{
  "$schema": "https://schema.railway.app/railway-schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "uv run cortex trigger-refresh --only congress,discover,volatility",
    "cronSchedule": "0 6 * * *",
    "restartPolicyType": "NEVER"
  }
}
